selefra:
  registry:
    # Indicates that a github based registry is used
    type: github
    # The fullname of the registry repo
    repo: private-org/private-repo
    # Access to github's credentials, may be api token
    token: xxxx
    # "modules" or "providers", If not, the default is to use the registry for both
    scope: modules